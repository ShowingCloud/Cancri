<h1 class="page-header">
  编辑<%= t('activerecord.models.district') %>
  <small class="pull-right">
    <a href="<%= admin_photo_path(@video) %>" title="显示该<%= t('activerecord.models.demeanor') %>" class="btn btn-default">
      <i class="icon-eye-open icon-only"></i></a>
    <a href="<%= "#{admin_photos_path}?cod=#{@video.competition_id}" %>" title="<%= t('activerecord.models.demeanor') %>列表" class="btn btn-default">
      <i class="icon-list icon-only"></i></a>
  </small>
</h1>

<%= render 'form' %>
